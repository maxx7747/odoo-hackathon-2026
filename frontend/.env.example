# Base URL of the Django REST API. Leave as /api during local dev — Vite
# proxies it to http://127.0.0.1:8000 (see vite.config.js).
VITE_API_BASE_URL=/api

# Set to "false" once your Django endpoints are live to stop using mock data.
VITE_USE_MOCKS=true
